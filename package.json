{
  "name": "fii-dii-strategy-automation-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "pip3 install -r requirements.txt || pip install -r requirements.txt; npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
